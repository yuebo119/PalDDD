# 第三十三轮 · 修复验证轮报告（verify-2026-08-19-r33）

> 任务指令：完整修复，但**修复前必须先验证问题是否仍存在**（代码可能已被其他 agent 修改）；
> 已不存在的问题跳过并记录，不做无据修复。
> 基线：外仓 `2cb2607`（其他 agent 已提交 ITM-209~241 修复与回填，见 `c0d3ed2`）。
> 本轮工作区变更：19 文件，+196/-30 行（复核轮审计后另补 3 组行为回归测试并修正本报告 2 处失实，见第三节注）。
>
> ⚠️ **编号说明**：本轮代码注释中的 ITM-211/212/215~221 引用沿用修复会话自己的审计编号，
> 与 `action-items-2026-08-18-v3.md` 的同名 ITM 编号**含义不同**（v3 的 ITM-211=Projection 隔离、
> 212=Observer、219=Native 解压、220=重复 Handler 等）——grep 交叉引用时以文件名+行为为准，勿按编号对号。

## 一、验证结论总览

| 处置 | 数量 | 说明 |
|------|:--:|------|
| 验证后确认存在并修复（上一会话） | 10 | 详情见第二节 |
| 验证时发现已被其他 agent 修复 → 跳过 | 3 | action-items-v3 已标全完成，实测代码核实 |
| 实测复现无问题 → 跳过 | 1 | ITM-214（IdempotencyTests flaky），5×161 全绿 |
| 本轮新发现并修复 | 3 | ITM-219 文档失实 + 2 处上轮修改引入的编译错 |

## 二、已修复项（逐项验证证据）

### 2.1 上一会话修复的 10 组问题（本轮逐一复核仍在工作区，未提交）

| # | 文件 | 修复内容 | 验证方式 |
|---|------|----------|----------|
| 1 | `InMemoryOutboxStore.cs` | RequeueDeadAsync 补 `ct.ThrowIfCancellationRequested()` + retriedBy 256 截断（PD24 截断族） | 读码比对 InMemoryInbox 姊妹实现 |
| 2 | `InMemorySagaStateStore.cs` | 4 个异步方法 ct 对齐（契约：同步完成也须响应取消） | 全方法枚举核对 |
| 3 | `OutboxDbContext.cs`（EF） | RequeueDeadAsync retriedBy 截断 + 删除恒真条件 `if (!translated \|\| true)` | 读码发现死代码并清除 |
| 4 | `PostgreSqlOutboxDbContext.cs` | leaseDuration 边界检查（非正值拒绝） | 对照守卫族 |
| 5 | `SqliteOutboxDbContext.cs` | RequeueDeadAsync owner null/空白守卫 | 对照守卫族 |
| 6 | `KafkaBroker.cs` | consumer.Close() try-catch 包裹（Close 抛异常不再跳过 Dispose）+ DisposeAsync Interlocked 幂等 | 资源泄漏路径分析 |
| 7 | `Dispatcher.cs` | 核心分发路径全量 `ConfigureAwait(false)`（库代码不回捕调用方上下文） | 逐 await 枚举 |
| 8 | `JsonMessageSerializer.cs` | 非泛型 `Serialize(object, ...)` 补 ThreadStatic 缓冲池化（对齐泛型路径，消除每次分配 Utf8JsonWriter + ArrayBufferWriter） | 分配路径比对 |
| 9 | `EventStreamJsonLines.cs` | ITM-218：MaxLineChars=16MB 单行上限（超长无换行输入受控拒绝）+ 导出迭代 ConfigureAwait | 读码 + 常量核对 |
| 10 | `SqlitePerformanceOptimizer.cs` | OptimizeAsync 补 `connection.State` 守卫（已打开连接重复 Open 抛异常）——对照 MySqlPerformanceOptimizer 同款 | 读码比对 |

另含 7 处小修（同一验证原则下确认存在后修复）：
- `MySqlServiceCollectionExtensions.cs`：ITM-211 池扩容缺口——SET SESSION 仅覆盖池内首连接，XML doc 显式声明（行为修复需每物理连接 ConnectionLifeTime 轮换，属设计权衡留痕）
- `PostgreSqlReportHelper.cs`：ITM-212 CopyToCsvAsync CSV 公式注入无防护——服务器端 COPY 无法逐单元格转义，XML doc 声明 + 指引改用 ExportCsvAsync
- `PostgreSqlSharding.cs`：分片名生成补标识符验证（防注入）
- `StrategicDddAnalyzer.cs`：GetFullMetadataName 嵌套类型以 `+` 拼接（修复嵌套类诊断位置错误）
- `MessageRegistryGenerator.cs`：MessageInfo 手写 Equals 排除 Location（位置漂移不再击穿增量缓存；与 Enum/Identity 生成器策略对齐）
- `PostgreSqlPipeline.cs`：await using 构造表达式注释修正（.ConfigureAwait(false) 于非 Task 不合法）
- `ValueObject.cs`：RowVersion.Next() 改 `checked`（int.MaxValue 溢出显式失败优于静默回绕——回绕产生"旧版本"乐观锁令牌；对齐 IdentityGenerator ITM-099 语义）

### 2.2 跳过项（验证不存在 / 已被解决）

| # | 项 | 跳过证据 |
|---|------|----------|
| 1 | EF Outbox MarkProcessed/MarkDead 租约持有者守卫（P1） | 其他 agent 已在 `c0d3ed2` 前修复（commit `22ba82d`），实测代码为 **owner 匹配守卫**（`WHERE LockedBy IS NULL OR LockedBy == 原持有者`，`OutboxDbContext.cs:81/101`）。~~原报告称"含 LeaseVersion/FencingToken 匹配"~~——**复核更正：全仓 grep `FencingToken\|LeaseVersion` 零命中，单调 token 未实现**；owner 守卫已覆盖旧 worker 覆写新 owner 路径，fencing token 列属 ITM-210 未落地部分（见 action-items-v3 ITM-210 条目"未落地"注，需跨方言 DDL 专项轮） |
| 2 | PalORM PG Outbox 原子租约 FOR UPDATE SKIP LOCKED（P1） | ITM-210 修复已含子查询 SKIP LOCKED，实测确认 |
| 3 | 方言探针/测试 destructive 清理守卫（P0/P1 族） | ITM-208 修复落地——本轮全量测试中 PalORM 45 失败正是其 fail-closed 守卫生效的直接证据（无 Testcontainers 环境拒绝连接外部库） |
| 4 | **ITM-214 IdempotencyTests Activity 并行污染 flaky** | **实测 5 连跑全量 Integration.Tests（161 测试含 IdempotencyTests 13 个全并行）全部通过、零失败**。机理分析：RecordingActivityListener 有 StartTimeUtc >= 构造时间过滤；且断言形态为"至少一个匹配"（Count>=1 / First(谓词)），外来并行 activity 只增不减、不破坏谓词命中。无 [NotInParallel] 需求 |

### 2.3 本轮新修复（3 项）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `Saga.cs:126-135` | **ITM-219**：SagaManager XML doc 声称"需步骤自身配置 Timeout 才会被超时补偿"——**失实**。实证链：`ExecuteInterruptStep` 只改 Status 不改 CurrentState → IsTimedOut 的 `CurrentState == ExtractState(key)` 匹配确实成立、StepStartedAt 已记录、Timeout 技术上可配——**但** 所有 Store（InMemory/Dapper/PalORM/EFCore）的 `LeaseActiveSagasAsync` 只取回 `Status == Active`，中断态（AwaitingHumanDecision）**永远不被 CheckTimeoutsAsync 扫描**，配置 Timeout 也无人调用 IsTimedOut。超时兜底对中断态完全不可用 | 文档改为准确声明（行为不改——改行为属新功能设计，超出修复轮范围） |
| 2 | `PostgreSqlReportHelper.cs:190` | 上轮 ITM-212 声明引入 CS0419 编译错：cref `EscapeCsvSpan` 有 ReadOnlySpan\<char\> 与 string 两个重载，引用不明确 | cref 消歧义为 `EscapeCsvSpan(System.ReadOnlySpan{char})` |
| 3 | `MessageRegistryGenerator.cs:241` | 上轮缓存修复引入 CS0234 编译错：`System.HashCode` 在 netstandard2.0（该项目 TFM）不存在 | 改为 17/31 手写哈希（与 EnumGenerator/IdentityGenerator 既有策略对齐） |

## 三、回归验证（机械轴）

| 门禁 | 结果 |
|------|------|
| `dotnet build PalDDD.slnx` | **0 error / 0 warning**（修复 2 处编译错后） |
| 全量测试（16 项目） | **14 项目全绿共 885 测试 0 失败**（原报告 866 为快照时点数；复核轮补充 3 组回归测试 +6 用例、DI 层新增 7 用例后实跑复核为 885）；PalORM.Tests 45 失败 = ITM-208 fail-closed 守卫在无 Docker 环境的预期行为；Messaging.Integration 3 失败 = RabbitMQ broker 不可达（BrokerUnreachableException）+ 3 Kafka 环境跳过——**均为环境性，非代码回归**（复核轮实跑逐项复现一致） |
| IdempotencyTests flaky 探针 | 5×161 全绿（见 2.2 #4） |
| `dotnet format --verify-no-changes` | **0 issue** |

## 四、遗留与建议

1. **复核轮补充（2026-08-19 复核）**：19 文件修复经逐 hunk 审计确认全部为有价值修复（无机械/无效改动）；另补 3 组行为回归测试——`RowVersion.Next()` 溢出（PropertyTests）、`ShardedTableName.For/All` 标识符防注入（PostgreSqlShardingTests）、`InMemorySagaStateStore` 取消令牌响应（InMemoryStoreTests）。已随本报告一并提交。
2. **中断态超时兜底**：ITM-219 仅修文档。若需真实兜底，需设计"LeaseActiveSagasAsync 纳入 AwaitingHumanDecision + HumanDecisionDeadline 扫描"（action-items ITM-235 已记录该方向），属新功能。
3. **ITM-211 池扩容缺口**：文档声明已完成；行为级修复（每物理连接 session 轮换）建议在 MySQL Provider 专项轮处理。
4. **FencingToken 缺口**：ITM-210 的单调 LeaseVersion/fencing token 未实现（本报告 2.2 #1 已更正），建议与 MySQL 池扩容同轮或独立专项轮处理（涉及跨方言 DDL）。

# Pal.DDD 第三十一轮全量评审报告（`/review --full`）

> 基线：`79eadee`（三十轮 action-items 回填）
> 生成：2026-08-18 · 执行：四片并行子代理地毯逐行（片 3 通道失败一次重派）+ 主线程专项（三十轮修复 diff 敌对审查）+ 跨片不变式
> 系统状态：误判库 PD1-PD32（40 模式）+ 四评审轴

---

## 段 1：覆盖度声明

| 分片 | 文件数 | 行数 | 方式 |
|------|-------|------|------|
| 片 1 | 16 | ~6,300 | 逐文件逐行 Read + 辅助核实（ProjectionCheckpoint/SmartEnum 签名） |
| 片 2 | 28 | ~6,400 | 逐文件逐行 Read |
| 片 3（重派） | 43 | ~6,300 | 逐文件逐行 Read |
| 片 4 | 92 | ~5,970 | 逐文件逐行 Read（目录级全覆盖 + ls 补读） |
| **合计** | **179** | **~24,970** | 全部 Read 工具逐行真实读取，无缓存/记忆 |

**主线程专项**：`7ae860a`（三十轮修复）11 文件 diff 逐行敌对审查——ITM-191/192/193 修复质量通过；发现修复自身引入的 P3 边界 1 项（BulkCopy 入口 probe 使 extractor 首行被调用两次）。跨片不变式：SQLite 类型限定全仓对称 ✓（窗口扫描一次误报经人工读码排除）、截断族三处 2000 对称 ✓。

## 段 2：机械轴

| 防线 | 结果 |
|------|------|
| gate-check | **22/22** |
| verify-ai-system | **16/16**（V7 40 条） |
| tech-debt-scan | 20 通过 / 2 允许 / 0 失败 |
| doc-consistency | **10/10** |
| 方言实测轴 | **PG 20 + MySQL 20 全 PASS**（真库） |

## 段 3：发现汇总

- **P0：0 · P1：0 · P2：2 · P3：22**

### P2（2 项，主线程已核实）

| ID | 位置 | 问题 |
|----|------|------|
| ITM-200 | SagaProcessor.cs:154 | **补偿失败路径 Error 无截断**（列 2048）——补偿异常（AggregateException/FanOut 多异常消息可达 >2048）→ SaveChanges 抛 DbUpdateException → 被外层 catch 吞 → Saga 停留 Processing、租约过期重租重补偿 → **无限重试循环 + CompensationFailed 永远无法落库**。Inbox/Outbox 均有截断兜底，Saga 缺席（PD24 截断族漏网） |
| ITM-201 | PostgreSqlReportHelper.cs:270-275 | **CSV 公式注入无防护**（OWASP CSV Injection）——EscapeCsvSpan 只处理逗号/引号/换行，`= + - @` 前缀不设防；events 的 reason/actor_id 等操作者输入导出 CSV 经 Excel 打开可触发公式执行。报表导出是该类明确定位 |

### P3（22 项，代表项）

| 类别 | 项 |
|------|-----|
| 时间语义 | PalOrmIdempotency/Checkpoint 读侧 GetDateTime Kind=Unspecified 时区漂移、PalOrmSaga ReadSagaRow 同型、DomainEvent/SagaState Ulid 不受 TimeProvider 控制 |
| 截断/边界 | retriedBy audit 无长度上限（三实现均匀）、PostgreSqlJsonb 空数组 `array[]` 类型推断失败 |
| 并发/生命周期 | KafkaBroker Dispose 无订阅拦截（关停窗口泄漏）、InMemoryOutbox GetPending/InMemoryInbox Mark 无 ct（姊妹不对称）、ServiceRegistration 双实例（需 PD32 探针） |
| 声明失真 | DapperEventLog "流式" 注释 vs buffered 全物化、ProjectionProcessor TimeSpan.Zero 被静默替换 5 分钟、HandlerNotFound 头注释 400→404、PalOrm sync-over-async 隐式契约、NameAndVersionComparer 自反性 |
| 监控盲区 | Inbox/Idempotency handler OCE 传播无计数 |
| 生成语义 | EnumGenerator 非 TSelf 静态字段拼入注册 → CS1503 无定向诊断 |
| 配置误判 | mode=memory 仍整体子串 Contains |
| 其他 | CodeFix 首个字面量（attribute 参数误改）、EventLogReplaySource 计数语义、SagaCompensation Contains 引用相等、EndpointExtensions 裸 400 vs ProblemDetails、BulkCopy extractor 首行双调用（三十轮修复引入） |

## 段 4：趋势

- **P1 连续两轮清零**（29 轮 4 → 30 轮 1 → 31 轮 0），修复质量提升与防误报门生效
- 但 P2 仍为截断族/安全类：ITM-200 是 PD24 截断族的第三处漏网（Saga 路径从未入族），提示截断族需要机械枚举（tech-debt 建议）
- CSV 注入是首现的安全类发现——此前七流地毯对该文件（ReportHelper）以性能/类型审查为主，安全流覆盖应扩展到"导出/序列化边界"

## 段 5：局限声明

- 片 1 PalOrmSaga 时区项 [推断]（未验证 PalORM MySQL 写入格式），修复前先补探针
- 全量测试以既有 974/974 为基线（本轮评审未引入代码变更，无需重跑）
- 子代理通道三次重派成功（片 3），覆盖度完整

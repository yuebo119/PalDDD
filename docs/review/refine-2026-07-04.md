# Pal.DDD 代码精炼报告

> 精炼日期：2026-07-04  
> 精炼系统：v7.0（24 项·三类）  
> 基准 commit：`4459e23`

---

## 执行摘要

**精炼前**：178 源文件  
**精炼后**：178 源文件（文件数不变）  
**减代码**：~75 字符（5 处 `Array.Empty<T>()` → `[]`）  
**执行项**：1/24（M1），评估跳过 22/24，无适用场景 1/24（M6）

**结论**：项目已高度精炼。24 项扫描命中率极低（5 项可执行，19 项已是最优形态）。框架库代码天然倾向于惯用模式（`?? throw`、`ToFrozenDictionary()` 构建器），常规精炼操作空间有限。

---

## 逐项结果

### 一类：减法（5 项·全部评估）

| ID | 扫描 | 执行 | 说明 |
|:--:|:--:|:--:|------|
| A1 | 0 | — | 无 AssemblyInfo.cs |
| A2 | 0 | — | 无 GlobalUsings.cs |
| A3 | 2 | ❌ | `IdempotencyExecution.cs`（enum+record）、`IdempotencyPolicy.cs`（配置）各有独立语义，合并降低可发现性 |
| A4 | — | — | 已在审计中评估（全量引用图），零 0 引用私有方法 |
| A5 | 469 using | ✅ | `dotnet format analyzers --diagnostics=IDE0005` 自动清理 |

### 二类：现代化（10 项·1 执行·2 受阻·7 评估）

| ID | 扫描 | 执行 | 说明 |
|:--:|:--:|:--:|------|
| **M1** | 15 | ✅ 5 处 | `Array.Empty<T>()` → `[]`（见明细） |
| M2 | 3 | ❌ 🔴 | 主构造函数风险高（继承链 + ORM 兼容），不下沉 |
| M3 | 87 | ❌ 🔴 | `required` 影响 ORM 映射类（EF Core 需无参构造），不下沉 |
| M4 | — | — | 类型判断链已在 SmartEnum 等处使用 switch 表达式 |
| M5 | 0 | — | 无 `string.Format` |
| M6 | 19 | ❌ | 18 处已是惯用 `?? throw` 模式；1 处在 `netstandard2.0`（API 不可用） |
| M7 | — | — | 已有 `DomainEventEnumerable` ref struct 枚举器 |
| M8 | — | — | 值对象已使用 `readonly record struct` |
| M9 | — | — | .NET 11 未到 GA，Lock 类评估延后 |
| M10 | — | — | 无 `params T[]` 场景 |

### 三类：优化（9 项·0 执行·9 评估）

| ID | 扫描 | 执行 | 说明 |
|:--:|:--:|:--:|------|
| O1 | 5 | ❌ | 所有 `new Dictionary<>` 均为构建器（→`ToFrozenDictionary()`）或外部 API 传入（RabbitMQ headers / EF Core comparer） |
| O2 | 0 | — | 集合构造已预分配（`new(capacity)`） |
| O3 | 36 | ❌ | 多为非热路径或测试代码，不下沉 |
| O4 | — | — | 已有 |
| O5 | — | — | 非热路径不下沉 |
| O6 | 1 | ❌ | 1 处 `string +=` 为低频率日志构建，非热路径 |
| O7 | — | — | 无 ValueTask 多重 await |
| O8 | — | — | 已有泛型约束 |
| O9 | — | — | `Enum.ToString()` 已改用 `switch` 表达式 |

---

## M1 明细：Collection Expressions

| 文件 | 行 | 变更前 | 变更后 | 返回类型 |
|------|:--:|--------|--------|:--:|
| `SystemCompressor.cs` | 33 | `Array.Empty<byte>()` | `[]` | `byte[]` ✅ |
| `SystemCompressor.cs` | 76 | `Array.Empty<byte>()` | `[]` | `byte[]` ✅ |
| `DefaultSagaManager.cs` | 47 | `new(Array.Empty<SagaState>())` | `new([])` | `ValueTask<IReadOnlyList<SagaState>>` ✅ |
| `FanOutStep.cs` | 69 | `new(Array.Empty<TResult>(), Array.Empty<...>())` | `new([], Array.Empty<...>())` | `FanOutResult<TResult>` ✅ |
| `SagaTimeoutDetector.cs` | 55 | `Array.Empty<SagaStep>()` | `[]` | `IReadOnlyList<SagaStep>` ✅ |

**受阻**：6 处 `Compress()` 方法返回 `ReadOnlyMemory<byte>`，CS9174 不兼容集合表达式。保留 `Array.Empty<byte>()`。

---

## 验证

```
dotnet build PalDDD.slnx  →  0 警告 0 错误
dotnet test PalDDD.slnx   →  747 通过 0 失败 6 跳过
```

---

## 精炼后扫描

```
一类: A1=0 A2=0 A3=2 A5=已清理
二类: M1=10(剩余) M2=3 M3=87 M5=0 M6=19
三类: O1=5 O2=0 O3=36 O6=1
```

# ─────────────────────────────────────────────────────────────
# 🎯 Pal.DDD .gitignore — 全面覆盖 .NET + AOT + IDE + CI
# ─────────────────────────────────────────────────────────────

## ── .NET 构建产物 ──
bin/
obj/
*.nupkg
*.snupkg
*.nuget.props
*.nuget.targets
**/packages/

## ── Native AOT / 发布产物 ──
**/publish/
*.aotprofile
*.aotdetails

## ── IDE / 编辑器 ──
.vs/
.idea/
*.user
*.suo
*.userosscache
*.userprefs
*.csproj.user
*.DotSettings
*.DotSettings.user
*sln.docstates
~$*

## ── 测试 / 覆盖率 ──
**/TestResults/
test-results/
*.coverage
*.coveragexml
*.opencover.xml
BenchmarkDotNet.Artifacts/
**/BenchmarkDotNet.Artifacts/

# Verify 快照比对失败产物（比对未过时生成，审阅后应删除——VerifyChecks 自检要求）
*.received.*
*.received/

## ── 诊断 / 日志 ──
*.log
# GitHub Actions 运行器日志（手动下载的排查产物，不入库）
job_logs.txt
*.etl
*.etw
*.heapsnapshot
*.diagsession
*.trace
**/TraceResults/

## ── OS 元数据 ──
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db
*.lnk

## ── AI 工具产物（不入库）──
.serena/
.claude/
.codex/
# .githooks/ 已跟踪（ITM-671：pre-commit/pre-push hook 随仓分发）
# !.githooks/
repomix-output.md
**/repomix-output.md
tmp/
/.ai
/.agents
.depwire/
.zcode/

## ── 编辑器临时文件 ──
*~
*.swp
*.swo
*.bak
*.tmp
# Cortex MCP metadata
.cortex/
nul
test-file.txt
stryker-config.json
/.codebuddy
/.codebuddy
/nupkgs
# Native AOT publish 临时产物
/artifacts

# PalOrmSample 运行时临时数据库 + 意外本地数据库（WAL 伴随文件一并忽略）
palddd-palorm-sample.db
*.db
*.db-shm
*.db-wal

# 测试环境本地配置（含连接串/密码，不入 git）
appsettings.test.local.json
test/*/appsettings.json

# CI 并行测试循环的逐项目日志/退出码目录（ci.yml 的 Test (unit + integration) 步骤产生）
ci-test-results/

# 密钥与证书（审计 2026-09-20 S7：防强名称密钥/本地证书误提交的第二道防线）
*.snk
*.pfx
*.p12
*.keystore
*.jks
.env
.env.*
!.env.example

# AI 质量系统独立 git 仓库（不在 PalDDD 仓库中跟踪）
research/
.probe-dump/
BenchmarkDotNet.Artifacts/
artifacts-medium/
/data
